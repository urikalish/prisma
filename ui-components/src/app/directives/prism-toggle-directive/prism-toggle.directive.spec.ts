import { PrismToggleDirective } from './prism-toggle.directive';
import {Renderer2} from "@angular/core";

describe('PrismToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new PrismToggleDirective();
    expect(directive).toBeTruthy();
  });
});
