import {TabbableInputDirective} from "./tabbable-input.directive";

describe('TabbableInputDirective', () => {
  it('should create an instance', () => {
    const directive = new TabbableInputDirective();
    expect(directive).toBeTruthy();
  });
});
