import { ListNavigationDirective } from './list-navigation.directive';

describe('ListNavigationDirective', () => {
  it('should create an instance', () => {
    const directive = new ListNavigationDirective();
    expect(directive).toBeTruthy();
  });
});
