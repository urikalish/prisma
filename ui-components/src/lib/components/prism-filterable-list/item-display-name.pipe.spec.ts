import { ItemDisplayNamePipe } from './item-display-name.pipe';

describe('ItemDisplayNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ItemDisplayNamePipe();
    expect(pipe).toBeTruthy();
  });
});
