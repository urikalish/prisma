import { BoldQueryPipe } from './bold-query.pipe';

describe('BoldQueryPipe', () => {
  it('create an instance', () => {
    const pipe = new BoldQueryPipe();
    expect(pipe).toBeTruthy();
  });
});
