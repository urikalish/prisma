import { DetectChangesDirective } from './detect-changes.directive';

describe('DetectChangesDirective', () => {
  it('should create an instance', () => {
    const directive = new DetectChangesDirective();
    expect(directive).toBeTruthy();
  });
});
